#ifndef CALC_H
#define CALC_H

const char *calc_flow(char *str, unsigned long flow);
const char *calc_speed(char *str, unsigned long flow);

const char *calc_fee(char *str, unsigned fee);

#endif /* CALC_H */